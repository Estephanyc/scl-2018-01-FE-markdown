linea ejecutable

require mdkinks(la funcion).mdlinks
if (enlace de slack) {
    
}
convertir el arg a object true
function 
(params) {
    
}

module.exports =mdlinks
console.console.error(
    para instalar la biblioteca en el computador sin publicar

    //para instalar lalibreria en mi pagina
    sudo npm install -g 
    // testear cyando no manda argumento

);

module.exports = {
    mdlinks: mdlink,
    otro: otro
}
puedo exportar un objeto con mis funciones
y hacer require para cada uno

poner las funciones en un objeto
module.exports ={
    rut:rut
}

// cuando se recibe una carpeta el caso base es cuando encuentra un archivo  y si no llama la carpeta 
function fact (num){
    if(num ==0) return 1;
    return num * fact(num-1)
}